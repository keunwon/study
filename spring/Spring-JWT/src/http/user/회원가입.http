### 1. 회원 가입
POST {{host}}/auth/sign
Content-Type: application/json

{
  "username": "{{username}}",
  "password": "{{password}}",
  "email": "{{email}}",
  "nickname": "{{nickname}}",
  "name": "{{name}}"
}
