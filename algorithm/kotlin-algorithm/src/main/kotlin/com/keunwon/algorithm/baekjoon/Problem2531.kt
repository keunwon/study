package com.keunwon.algorithm.baekjoon

/**
 * Title: 회전 초밥
 * Level: 실버-1
 **/
class Problem2531 {
    fun solution(d: Int, k: Int, c: Int): Int {
        return 0
    }
}

fun main() {
    val (n, d, k, c) = readLine()!!.split(" ").map { it.toInt() }
    Problem2531().solution(d, k, c).also { println(it) }
}
