package com.keunwon.algorithm.againresolve

class ALessons92345 {
    fun solution(board: Array<IntArray>, aloc: IntArray, bloc: IntArray): Int {
        return 0
    }
}


fun main() {
    val board = arrayOf(
        intArrayOf(1, 1, 1),
        intArrayOf(1, 1, 1),
        intArrayOf(1, 1, 1),
    )
    val aloc = intArrayOf(1, 0)
    val bloc = intArrayOf(1, 2)

    ALessons92345().solution(board, aloc, bloc).also { println(it) }
}
